[project]
name = "py-agent-tools"
version = "0.1.0"
description = "Reusable built-in tools and sandbox policy primitives"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
